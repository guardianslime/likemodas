# likemodas/nixpacks.toml (CORREGIDO Y SIMPLIFICADO)

providers = ["python"]
[python]
version = "3.11"

[phases.setup]
# Mantenemos las dependencias para la librería Pillow, pero quitamos las de zbar.
aptPkgs = ["libjpeg-dev", "zlib1g-dev"]

[start]
cmd = "sh ./start.sh"