providers = ["python"]
[python]
version = "3.11"

[phases.setup]
# Mantenemos solo las dependencias básicas para Pillow, que son inofensivas.
aptPkgs = ["libjpeg-dev", "zlib1g-dev"]

[start]
cmd = "sh ./start.sh"